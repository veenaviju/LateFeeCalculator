namespace LateFeeCalculatorService
{
    public class LateFeeCalculator
    {
        public decimal Calculate(decimal amount, DateTime dueDate) 
        {
            decimal feeAmount = 0;
            return feeAmount; 
        }
    }
}